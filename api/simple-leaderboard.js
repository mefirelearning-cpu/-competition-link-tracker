import { query } from "../lib/db.js";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const js=v=>JSON.stringify(v).replace(/</g,"\\u003c");
export default async function handler(req,res){
 const raw=String(req.query?.path||"");const m=raw.match(/^leaderboard\/([^/]+)$/);if(!m){res.statusCode=404;return res.end("Not found");}
 const id=decodeURIComponent(m[1]);const cr=await query("SELECT id,name,description,cover_url,settings FROM competitions WHERE id=$1 LIMIT 1",[id]);const c=cr.rows[0];if(!c){res.statusCode=404;return res.end("Competition introuvable");}
 const rr=await query(`SELECT p.id participant_id,p.pseudonym name,cp.total_points_cache points,cp.unique_clicks_cache uniques FROM competition_participants cp JOIN participants p ON p.id=cp.participant_id WHERE cp.competition_id=$1 AND cp.status='active' AND p.status='active' ORDER BY cp.total_points_cache DESC,cp.valid_clicks_cache DESC,cp.joined_at ASC`,[id]);
 const rows=rr.rows;const prizes=String(c.settings?.prizes||c.settings?.rewards||c.description||"Récompenses annoncées par l’organisateur");
 let history=[];try{const hr=await query(`SELECT bucket_at,participant_id,points FROM rank_snapshots WHERE competition_id=$1 ORDER BY bucket_at ASC`,[id]);history=hr.rows;}catch{}
 const labels=[...new Set(history.map(x=>new Date(x.bucket_at).toISOString()))];const leaders=rows.slice(0,5);const series=leaders.map(r=>({name:r.name,values:labels.map(t=>{const hit=history.filter(x=>x.participant_id===r.participant_id&&new Date(x.bucket_at).toISOString()<=t).at(-1);return Number(hit?.points||0);})}));
 if(labels.length<2&&rows.length){labels.push(new Date().toISOString());series.forEach((s,i)=>s.values.push(Number(rows[i]?.points||0)));}
 const list=rows.map((r,i)=>{const ahead=i?Number(rows[i-1].points||0):null;const gap=ahead===null?0:Math.max(1,ahead-Number(r.points||0)+1);return `<div class="rank ${i<3?'top':''}"><div class="pos">${i+1}</div><div class="who"><b>${esc(r.name)}</b><small>${Number(r.uniques||0)} personnes touchées${i?` · <strong>${gap} pt${gap>1?'s':''}</strong> pour passer #${i}`:' · leader actuel'}</small></div><div class="pts">${Number(r.points||0)}<small>PTS</small></div></div>`}).join("")||'<div class="empty">Le classement commencera dès l’ajout des participants.</div>';
 const cover=c.cover_url?`<img class="cover" src="${esc(c.cover_url)}" alt="Affiche de la compétition">`:`<div class="coverFallback">${esc(c.name).slice(0,1).toUpperCase()}</div>`;
 const html=`<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#090b0a"><title>${esc(c.name)} · Classement</title><style>*{box-sizing:border-box}body{margin:0;background:#090b0a;color:#f5f7f5;font-family:Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.wrap{width:min(980px,calc(100% - 28px));margin:auto;padding:20px 0 50px}.hero{display:grid;grid-template-columns:minmax(220px,.72fr) 1.28fr;background:#131614;border:1px solid #282d2a;border-radius:28px;overflow:hidden;min-height:310px}.cover{width:100%;height:100%;max-height:430px;object-fit:cover}.coverFallback{min-height:310px;display:grid;place-items:center;background:radial-gradient(circle at 30% 20%,#34432b,#121512 65%);font-size:110px;font-weight:950;color:#a7ff4f}.heroText{padding:34px;display:flex;flex-direction:column;justify-content:center}.eyebrow{color:#a7ff4f;font-weight:850;text-transform:uppercase;letter-spacing:.13em;font-size:12px}.hero h1{font-size:clamp(38px,7vw,72px);line-height:.92;letter-spacing:-.055em;margin:10px 0 18px}.prize{background:#0c0e0d;border:1px solid #2c322e;border-radius:16px;padding:15px;color:#cdd2cf;line-height:1.45}.prize b{color:#fff}.sectionHead{display:flex;align-items:end;justify-content:space-between;gap:12px;margin:28px 3px 12px}.sectionHead h2{font-size:25px;margin:0}.sectionHead span{color:#737b76;font-size:12px}.chartCard{background:#131614;border:1px solid #282d2a;border-radius:22px;padding:18px}.chartWrap{height:240px;position:relative}.chartEmpty{height:100%;display:grid;place-items:center;color:#777f7a;text-align:center}.legend{display:flex;flex-wrap:wrap;gap:8px;margin-top:10px}.legend span{font-size:11px;color:#9ba29e;border:1px solid #2d332f;border-radius:999px;padding:5px 8px}.rank{display:grid;grid-template-columns:48px 1fr auto;align-items:center;gap:12px;background:#131614;border:1px solid #282d2a;border-radius:18px;padding:15px;margin:8px 0}.rank.top{background:linear-gradient(110deg,#182018,#131614 58%)}.pos{width:38px;height:38px;border-radius:12px;background:#202521;display:grid;place-items:center;font-weight:950}.top .pos{color:#a7ff4f}.who{min-width:0}.who b{font-size:17px}.who small{display:block;color:#7f8782;margin-top:4px;line-height:1.35}.who strong{color:#a7ff4f}.pts{font-size:25px;font-weight:950;text-align:right}.pts small{display:block;font-size:9px;letter-spacing:.1em;color:#777f7a}.empty{background:#131614;border-radius:18px;padding:20px;color:#8d9590}.foot{text-align:center;color:#5f6662;font-size:11px;margin-top:22px}@media(max-width:680px){.wrap{width:calc(100% - 14px);padding-top:7px}.hero{grid-template-columns:1fr;border-radius:22px}.cover{height:auto;max-height:300px}.coverFallback{min-height:180px}.heroText{padding:21px}.hero h1{font-size:clamp(36px,13vw,56px)}.chartCard{padding:12px}.chartWrap{height:210px}.rank{grid-template-columns:40px 1fr auto;padding:12px 10px}.pos{width:34px;height:34px}.who b{font-size:16px}.who small{font-size:11px}.pts{font-size:21px}}</style></head><body><main class="wrap"><section class="hero">${cover}<div class="heroText"><div class="eyebrow">Classement officiel</div><h1>${esc(c.name)}</h1><div class="prize"><b>Lots à gagner</b><br>${esc(prizes)}</div></div></section><div class="sectionHead"><h2>Progression</h2><span>Top 5 · évolution des points</span></div><section class="chartCard"><div class="chartWrap"><canvas id="chart"></canvas><div class="chartEmpty" id="empty" hidden>La courbe apparaîtra dès que l’historique de points sera disponible.</div></div><div class="legend" id="legend"></div></section><div class="sectionHead"><h2>Classement</h2><span>${rows.length} participant${rows.length>1?'s':''}</span></div>${list}<div class="foot">Mise à jour selon les points attribués par l’organisateur.</div></main><script>const labels=${js(labels)},series=${js(series)};const canvas=document.getElementById('chart'),empty=document.getElementById('empty'),legend=document.getElementById('legend');if(!labels.length||!series.length){canvas.hidden=true;empty.hidden=false}else{const colors=['#a7ff4f','#f3f5f3','#9ba29e','#69716c','#414743'];series.forEach((s,i)=>legend.insertAdjacentHTML('beforeend','<span>'+s.name+' · '+(s.values.at(-1)||0)+' pts</span>'));function draw(){const dpr=devicePixelRatio||1,w=canvas.clientWidth,h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;const x=canvas.getContext('2d');x.scale(dpr,dpr);x.clearRect(0,0,w,h);const pad={l:34,r:10,t:14,b:24};const max=Math.max(10,...series.flatMap(s=>s.values));x.strokeStyle='#262b28';x.lineWidth=1;for(let i=0;i<5;i++){const y=pad.t+(h-pad.t-pad.b)*i/4;x.beginPath();x.moveTo(pad.l,y);x.lineTo(w-pad.r,y);x.stroke()}series.forEach((s,si)=>{x.strokeStyle=colors[si%colors.length];x.lineWidth=si===0?2.6:1.6;x.beginPath();s.values.forEach((v,i)=>{const xx=pad.l+(w-pad.l-pad.r)*(labels.length===1?1:i/(labels.length-1));const yy=h-pad.b-(h-pad.t-pad.b)*(Number(v)||0)/max;i?x.lineTo(xx,yy):x.moveTo(xx,yy)});x.stroke()});x.fillStyle='#707873';x.font='10px sans-serif';x.fillText('0',8,h-pad.b+3);x.fillText(String(max),8,pad.t+3)}draw();addEventListener('resize',draw)}</script></body></html>`;
 res.statusCode=200;res.setHeader("Content-Type","text/html; charset=utf-8");res.setHeader("Cache-Control","no-store");res.end(html);
}
