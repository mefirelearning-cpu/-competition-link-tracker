# Simple V1 ready

This branch exposes only the minimal launch workflow through Vercel routes: admin competition selection, participant link creation, referral tracking, manual point adjustment, and public leaderboard.

Legacy code remains in the repository so existing data and future features are not destroyed, but legacy routes are no longer exposed by the launch routing configuration.
